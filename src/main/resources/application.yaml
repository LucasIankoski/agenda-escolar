spring:
  application:
    name: agenda-api
  datasource:
    url: jdbc:postgresql://localhost:5436/diary
    username: admin
    password: agenda
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

server:
  port: 8080

jwt:
  secret: "uma-chave-bem-grande-com-mais-de-32-chars-123456"
  expiration: 86400000
