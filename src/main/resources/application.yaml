spring:
  application:
    name: ${APP_NAME:agenda-api}

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5436/diary}
    username: ${DB_USERNAME:admin}
    password: ${DB_PASSWORD:agenda}

  servlet:
    multipart:
      max-file-size: ${MULTIPART_MAX_FILE_SIZE:10MB}
      max-request-size: ${MULTIPART_MAX_REQUEST_SIZE:50MB}

  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:validate}
    show-sql: ${JPA_SHOW_SQL:false}
    properties:
      hibernate:
        format_sql: ${HIBERNATE_FORMAT_SQL:true}

  flyway:
    enabled: ${FLYWAY_ENABLED:true}
    locations: ${FLYWAY_LOCATIONS:classpath:db/migration}
    baseline-on-migrate: ${FLYWAY_BASELINE_ON_MIGRATE:true}

server:
  port: ${SERVER_PORT:8080}

jwt:
  secret: ${JWT_SECRET:uma-chave-bem-grande-com-mais-de-32-chars-123456}
  expiration: ${JWT_EXPIRATION:86400000}

app:
  super-admin:
    name: ${SUPER_ADMIN_NAME:admin}
    email: ${SUPER_ADMIN_EMAIL:admin@agenda.com}
    password: ${SUPER_ADMIN_PASSWORD:12345}
  tenant:
    default-school-name: ${DEFAULT_SCHOOL_NAME:Escola Padrao}
    default-school-slug: ${DEFAULT_SCHOOL_SLUG:default}
  gallery:
    storage:
      root-path: ${GALLERY_STORAGE_ROOT:storage/student-gallery}
